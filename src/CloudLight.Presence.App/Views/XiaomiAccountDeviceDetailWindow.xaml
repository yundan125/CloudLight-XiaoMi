<UserControl x:Class="CloudLight.Presence.App.Views.XiaomiAccountDeviceDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" CanContentScroll="False">
    <StackPanel Margin="{DynamicResource PagePadding}">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding StatusMark}" Foreground="{Binding StatusColor}" FontSize="20" Margin="0,0,9,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Name}" Style="{DynamicResource PageTitleStyle}" TextTrimming="CharacterEllipsis"/>
          </StackPanel>
          <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="{Binding StatusText}" FontWeight="SemiBold"/>
            <TextBlock Text=" · " Foreground="{DynamicResource SubtleBrush}"/>
            <TextBlock Text="{Binding LocationText}" Style="{DynamicResource MutedTextStyle}"/>
            <TextBlock Text=" · " Foreground="{DynamicResource SubtleBrush}"/>
            <TextBlock Text="{Binding ModelText}" Style="{DynamicResource MutedTextStyle}" TextTrimming="CharacterEllipsis"/>
          </StackPanel>
        </StackPanel>
        <Button x:Name="ReturnToDevicesButton" Grid.Column="1" Content="返回设备" Style="{DynamicResource SecondaryButtonStyle}" Command="{Binding DataContext.ReturnToDevicesCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Margin="0"/>
      </Grid>

      <Border Grid.Row="1" Style="{DynamicResource EmptyStateCardStyle}" Background="{DynamicResource WarningSoftBrush}" BorderBrush="{DynamicResource WarningBorderBrush}" Margin="0,20,0,0" Padding="14" Visibility="{Binding IsOffline, Converter={StaticResource BooleanToVisibility}}">
        <TextBlock Text="{Binding OfflineHint}" Foreground="{DynamicResource WarningBrush}" TextWrapping="Wrap"/>
      </Border>

      <Grid Margin="0,22,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <TextBlock Text="设备能力" Style="{DynamicResource SectionTitleStyle}"/>
          <TextBlock Text=" · " Foreground="{DynamicResource SubtleBrush}" Margin="7,0,0,0"/>
          <TextBlock Text="{Binding CapabilitySummary}" Style="{DynamicResource MutedTextStyle}" VerticalAlignment="Center"/>
        </StackPanel>
        <Button Grid.Column="1" Content="刷新能力" Command="{Binding RefreshCommand}" Style="{DynamicResource SecondaryButtonStyle}" Margin="12,0,0,0"/>
      </Grid>

      <TextBlock Text="设备状态" Style="{DynamicResource SectionTitleStyle}" Margin="0,28,0,12"/>
      <Border Style="{DynamicResource CardStyle}">
        <StackPanel>
          <ItemsControl ItemsSource="{Binding Services}" Visibility="{Binding HasReadableProperties, Converter={StaticResource BooleanToVisibility}}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <StackPanel Margin="0,0,0,17" Visibility="{Binding HasReadableProperties, Converter={StaticResource BooleanToVisibility}}">
                  <TextBlock Text="{Binding DisplayName}" Style="{DynamicResource CardTitleStyle}" FontSize="16" Margin="0,0,0,8"/>
                  <ItemsControl ItemsSource="{Binding ReadableProperties}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Grid Margin="0,5">
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="230"/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="80"/>
                          </Grid.ColumnDefinitions>
                          <TextBlock Text="{Binding DisplayName}" Style="{DynamicResource MutedTextStyle}"/>
                          <TextBlock Grid.Column="1" Text="{Binding CurrentValueText}" FontWeight="SemiBold"/>
                          <TextBlock Grid.Column="2" Text="{Binding UnitText}" Style="{DynamicResource MutedTextStyle}"/>
                        </Grid>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>
                </StackPanel>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <Border Style="{DynamicResource EmptyStateCardStyle}" Visibility="{Binding HasNoReadableProperties, Converter={StaticResource BooleanToVisibility}}">
            <StackPanel HorizontalAlignment="Center">
              <TextBlock Text="还没有可读取的状态" Style="{DynamicResource CardTitleStyle}" HorizontalAlignment="Center"/>
              <TextBlock Text="设备暂未提供可展示的只读属性。" Style="{DynamicResource MutedTextStyle}" Margin="0,7,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
          </Border>
        </StackPanel>
      </Border>

      <TextBlock Text="设备控制" Style="{DynamicResource SectionTitleStyle}" Margin="0,28,0,12"/>
      <Border Style="{DynamicResource CardStyle}">
        <StackPanel>
          <ItemsControl ItemsSource="{Binding WritableProperties}" Visibility="{Binding HasWritableProperties, Converter={StaticResource BooleanToVisibility}}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Grid Margin="0,4,0,16">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="230"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>
                  <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding DisplayName}" Style="{DynamicResource CardTitleStyle}" FontSize="15"/>
                    <TextBlock Text="{Binding OperationText}" Style="{DynamicResource MutedTextStyle}" FontSize="12" Margin="0,4,0,0"/>
                  </StackPanel>
                  <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox Content="{Binding BooleanControlText}" IsChecked="{Binding BoolValue, Mode=OneWay}" Command="{Binding ToggleCommand}" IsEnabled="{Binding CanOperate}" Style="{DynamicResource ToggleSwitchStyle}" Visibility="{Binding IsBoolean, Converter={StaticResource BooleanToVisibility}}"/>
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsEnum, Converter={StaticResource BooleanToVisibility}}">
                      <ComboBox Width="190" ItemsSource="{Binding ValueOptions}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedOption, Mode=TwoWay}" IsEnabled="{Binding CanOperate}" Style="{DynamicResource ComboBoxStyle}"/>
                      <Button Content="应用" Command="{Binding ApplyCommand}" IsEnabled="{Binding CanOperate}" Style="{DynamicResource SecondaryButtonStyle}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <StackPanel Visibility="{Binding IsNumber, Converter={StaticResource BooleanToVisibility}}" MinWidth="300">
                      <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="70"/></Grid.ColumnDefinitions>
                        <Slider Minimum="{Binding MinimumDouble}" Maximum="{Binding MaximumDouble}" Value="{Binding SliderValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TickFrequency="{Binding StepDouble}" IsSnapToTickEnabled="True" IsEnabled="{Binding CanOperate}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding DraftNumberText}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0"/>
                      </Grid>
                      <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="{Binding UnitText}" Style="{DynamicResource MutedTextStyle}" VerticalAlignment="Center"/>
                        <Button Content="应用" Command="{Binding ApplyCommand}" IsEnabled="{Binding CanOperate}" Style="{DynamicResource SecondaryButtonStyle}" Margin="10,0,0,0"/>
                      </StackPanel>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsString, Converter={StaticResource BooleanToVisibility}}">
                      <TextBox Width="220" Text="{Binding DraftText, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding CanOperate}" Style="{DynamicResource TextBoxStyle}"/>
                      <Button Content="保存" Command="{Binding ApplyCommand}" IsEnabled="{Binding CanOperate}" Style="{DynamicResource SecondaryButtonStyle}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="此类型暂不支持编辑" Style="{DynamicResource MutedTextStyle}" Visibility="{Binding IsUnsupportedEditor, Converter={StaticResource BooleanToVisibility}}" VerticalAlignment="Center"/>
                  </StackPanel>
                  <TextBlock Grid.Column="2" Text="{Binding DisabledText}" Foreground="{DynamicResource WarningBrush}" FontSize="12" VerticalAlignment="Center" MaxWidth="170" TextWrapping="Wrap" Margin="12,0,0,0"/>
                </Grid>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <Border Style="{DynamicResource EmptyStateCardStyle}" Visibility="{Binding HasNoWritableProperties, Converter={StaticResource BooleanToVisibility}}">
            <StackPanel HorizontalAlignment="Center">
              <TextBlock Text="没有可编辑的属性" Style="{DynamicResource CardTitleStyle}" HorizontalAlignment="Center"/>
              <TextBlock Text="{Binding NoControlsText}" Style="{DynamicResource MutedTextStyle}" Margin="0,7,0,0" HorizontalAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>
          </Border>
        </StackPanel>
      </Border>

      <TextBlock Text="更多操作" Style="{DynamicResource SectionTitleStyle}" Margin="0,28,0,12"/>
      <Border Style="{DynamicResource CardStyle}">
        <StackPanel>
          <ItemsControl ItemsSource="{Binding Actions}" Visibility="{Binding HasActions, Converter={StaticResource BooleanToVisibility}}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Grid Margin="0,3,0,14">
                  <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                  <StackPanel>
                    <TextBlock Text="{Binding DisplayName}" Style="{DynamicResource CardTitleStyle}" FontSize="15"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                      <TextBlock Text="{Binding ServiceName}" Style="{DynamicResource MutedTextStyle}" FontSize="12"/>
                      <TextBlock Text=" · 含参数" Style="{DynamicResource MutedTextStyle}" FontSize="12" Visibility="{Binding HasInputArguments, Converter={StaticResource BooleanToVisibility}}"/>
                      <TextBlock Text=" · 需要二次确认" Foreground="{DynamicResource WarningBrush}" FontSize="12" Visibility="{Binding RequiresConfirmation, Converter={StaticResource BooleanToVisibility}}"/>
                    </StackPanel>
                    <TextBlock Text="{Binding OperationText}" Style="{DynamicResource MutedTextStyle}" FontSize="12" Margin="0,4,0,0"/>
                  </StackPanel>
                  <Button Grid.Column="1" Content="执行" Command="{Binding ExecuteCommand}" IsEnabled="{Binding CanOperate}" Style="{DynamicResource SecondaryButtonStyle}" VerticalAlignment="Center"/>
                </Grid>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <Border Style="{DynamicResource EmptyStateCardStyle}" Visibility="{Binding HasNoActions, Converter={StaticResource BooleanToVisibility}}">
            <StackPanel HorizontalAlignment="Center">
              <TextBlock Text="暂无可执行操作" Style="{DynamicResource CardTitleStyle}" HorizontalAlignment="Center"/>
              <TextBlock Text="设备能力描述中没有可安全执行的操作。" Style="{DynamicResource MutedTextStyle}" Margin="0,7,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
          </Border>
        </StackPanel>
      </Border>

      <StackPanel Visibility="{Binding HasEvents, Converter={StaticResource BooleanToVisibility}}">
        <TextBlock Text="支持的事件" Style="{DynamicResource SectionTitleStyle}" Margin="0,28,0,12"/>
        <Border Style="{DynamicResource CardStyle}">
          <StackPanel>
            <TextBlock Text="事件仅作为设备能力信息展示，不会伪造成可执行按钮。" Style="{DynamicResource MutedTextStyle}" Margin="0,0,0,9"/>
            <ItemsControl ItemsSource="{Binding Events}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Grid Margin="0,4">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding DisplayName}"/>
                    <TextBlock Grid.Column="1" Text="{Binding ServiceName}" Style="{DynamicResource MutedTextStyle}"/>
                  </Grid>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </Border>
      </StackPanel>

      <Border Background="{DynamicResource WarningSoftBrush}" BorderBrush="{DynamicResource WarningBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="13" Margin="0,22,0,0">
        <Border.Style>
          <Style TargetType="Border">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers><DataTrigger Binding="{Binding DiagnosticMessage}" Value=""><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers>
          </Style>
        </Border.Style>
        <TextBlock Text="{Binding DiagnosticMessage}" Foreground="{DynamicResource WarningBrush}" TextWrapping="Wrap"/>
      </Border>

      <Expander Header="高级设备信息" Style="{DynamicResource CloudLightExpanderStyle}" Margin="0,26,0,20">
        <Border Style="{DynamicResource CardStyle}" Margin="0,10,0,0">
          <StackPanel>
            <Grid>
              <Grid.ColumnDefinitions><ColumnDefinition Width="150"/><ColumnDefinition/></Grid.ColumnDefinitions>
              <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
              <TextBlock Text="型号" Style="{DynamicResource FieldLabelStyle}"/><TextBlock Grid.Column="1" Text="{Binding ModelText}"/>
              <TextBlock Grid.Row="1" Text="设备 ID" Style="{DynamicResource FieldLabelStyle}"/><TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Device.Did}"/>
              <TextBlock Grid.Row="2" Text="Spec 类型" Style="{DynamicResource FieldLabelStyle}"/><TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Device.SpecType}"/>
              <TextBlock Grid.Row="3" Text="服务数量" Style="{DynamicResource FieldLabelStyle}"/><TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Services.Count}"/>
            </Grid>
            <ItemsControl ItemsSource="{Binding Services}" Margin="0,17,0,0">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <StackPanel Margin="0,0,0,14">
                    <TextBlock Text="{Binding TechnicalText}" Style="{DynamicResource MutedTextStyle}"/>
                    <ItemsControl ItemsSource="{Binding AllProperties}" Margin="12,5,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding TechnicalText}" Style="{DynamicResource MutedTextStyle}" FontSize="12" Margin="0,2"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <ItemsControl ItemsSource="{Binding Actions}" Margin="12,2,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding TechnicalText}" Style="{DynamicResource MutedTextStyle}" FontSize="12" Margin="0,2"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                    <ItemsControl ItemsSource="{Binding Events}" Margin="12,2,0,0"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding TechnicalText}" Style="{DynamicResource MutedTextStyle}" FontSize="12" Margin="0,2"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                  </StackPanel>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </Border>
      </Expander>

      <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
        <TextBlock Text="{Binding LoadingText}" Style="{DynamicResource MutedTextStyle}" TextWrapping="Wrap"/>
        <TextBlock Text=" · " Foreground="{DynamicResource SubtleBrush}"/>
        <TextBlock Text="{Binding OperationStatus}" Style="{DynamicResource MutedTextStyle}" TextWrapping="Wrap"/>
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</UserControl>
